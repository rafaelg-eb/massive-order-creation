
```
# Create the config file:
cp config.example.json config.json

# Configure with your API key and event settings:
vi config.json

# Run the containers (by default, 5 instances):
sudo docker-compose up --build
```
